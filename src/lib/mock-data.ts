// Export empty collections since the app must exclusively use real synchronized data
export const patients: any[] = []

export const mockDashboardStats = {
  scheduledToday: 0,
  completedRecords: 0,
  newPatients: 0,
}
